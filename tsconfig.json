{
    "compilerOptions": {
      // === Language & Module Settings ===
      "target": "ES2022",                // Output JavaScript version (Cloudflare Workers run on modern V8, ES2022 is safe)
      "module": "ESNext",                // Use modern ES module output
      "moduleResolution": "Bundler",     // Recommended for Workers with Wrangler (bundler style resolution)
  
      // === Type Checking ===
      "types": ["@cloudflare/workers-types"], // Load Cloudflare Worker environment types (caches.default, ExecutionContext, etc.)
      "lib": ["ES2022", "WebWorker"],         // Include standard ES features and WebWorker APIs
  
      // === Strictness & Safety ===
      "strict": true,                    // Enable all strict type checks
      "forceConsistentCasingInFileNames": true, // Disallow inconsistent file name casing
      "skipLibCheck": true,              // Skip type checking of declaration files (*.d.ts) for faster builds
  
      // === Interoperability ===
      "esModuleInterop": true,           // Allow default imports from CommonJS modules
      "allowSyntheticDefaultImports": true, // Allow 'import x from y' when module exports with 'module.exports'
  
      // === Paths & Output ===
      "rootDir": "src",                  // Where your TypeScript source code lives
      "outDir": "dist",                  // Where compiled JavaScript will be output
      "noEmit": false                    // Allow emitting compiled JS (Wrangler usually handles bundling, so this may not matter)
    },
    "include": ["src"],                  // Only include files inside /src
    "exclude": ["node_modules", "dist"]  // Exclude build artifacts and dependencies
  }
  